# LaTeX build artifacts
*.aux
*.log
*.nav
*.out
*.snm
*.toc
*.vrb
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.blg
*.bcf
*.run.xml

# Build output at repo root -- the published copy that the
# website serves lives in docs/slides/, committed on purpose.
/*.pdf

# macOS
.DS_Store

# Not published: preliminary Q&A PDFs, kept locally for reference only
/QnA/

# Manim build artifacts and rendered output -- the published copy that
# the website serves lives in docs/videos/, committed on purpose.
videos/*/media/
videos/*/__pycache__/
videos/*/*.mp4
